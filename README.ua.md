# Mini SaaS Subscriptions API

Production-орієнтований backend MVP для SaaS-платформи з підписками на програмне забезпечення.

Проєкт реалізований як тестове завдання на позицію **Backend Developer (Node.js)**  
та спрямований на демонстрацію коректної бізнес-логіки, чистої архітектури
та надійного API без надмірного ускладнення.

📄 **Оригінальний опис завдання:**  
👉 [ASSIGNMENT.md](./ASSIGNMENT.md)

🧠 **Теоретичне завдання:**  
👉 [THEORY.md](./THEORY.md)  
👉 [THEORY_UA.md](./THEORY_UA.md)

---

## Технологічний стек

- **Node.js / TypeScript**
- **NestJS**
- **PostgreSQL**
- **Prisma ORM (v7, config-first)**
- **JWT-автентифікація**
- **Docker (Postgres)**
- **Swagger UI**
- **Jest (unit-тести лише для pricing-логіки)**

---

## Загальний огляд

API реалізує типовий SaaS-flow роботи з підписками:

1. Реєстрація та автентифікація користувачів
2. Розрахунок вартості підписки
3. Створення підписки з ініціалізацією платежу
4. Захист від повторних запитів через ідемпотентність
5. Отримання даних про власні підписки

Підтвердження платежів через webhooks навмисно не реалізоване,
оскільки це виходить за межі тестового завдання.
Водночас модель даних і flow підготовлені до такого розширення в реальному продукті.

---

## User Flow

```

Register → Login → Calculate Price → Create Subscription

```

---

## Архітектурні рішення

- Проєкт реалізовано як **монолітний MVP**, оскільки масштаб і вимоги тестового завдання не потребують мікросервісної архітектури.
- Повний DDD навмисно не застосовувався — логіка організована навколо модулів і сервісів NestJS для збереження простоти та читабельності.
- Бізнес-логіка винесена в сервіси, контролери відповідають лише за HTTP-шар.
- Інфраструктурні деталі (Prisma, платіжні провайдери) ізольовані від доменної логіки.
- Платіжні провайдери реалізовані як mock-сервіси, оскільки реальна інтеграція не вимагалася завданням.
- Для грошових розрахунків використовується Prisma.Decimal з округленням лише на фінальних значеннях.
- Ініціалізація платежу виконується **поза DB-транзакцією**, щоб не утримувати блокування БД під час мережевого I/O та зменшити ризики таймаутів або дедлоків.
- UUID використані як primary key для безпеки публічних API, запобігання перебору ідентифікаторів та кращої масштабованості.
- Enum-и застосовані для невеликих стабільних наборів значень (billingPeriod, status тощо) для типобезпеки та простоти запитів.
- Seed-скрипт реалізований ідемпотентно та працює напряму з Prisma, оскільки ініціалізує статичні reference-дані.
- Unit-тести написані лише для PricingService, як для найбільш критичної та детермінованої частини бізнес-логіки.
- Swagger використовується для документування API та перевірки контрактів.


### Graceful shutdown

Застосунок підтримує graceful shutdown, що дозволяє коректно завершувати роботу під час рестартів, деплою або rollback (Docker / CI / Kubernetes).

У `main.ts` увімкнено:

- `app.enableShutdownHooks()`

Під час завершення роботи застосунку:

- коректно закриваються всі з’єднання з базою даних
- PrismaClient та PostgreSQL connection pool завершують роботу без примусового розриву

Це дозволяє:
- уникнути витоків з’єднань
- безпечно виконувати деплой та rollback
- підвищити стабільність застосунку в production-середовищі

---

### Робота з базою даних: Prisma + PostgreSQL Pool (config-first)

У проєкті використовується Prisma ORM v7 у режимі config-first з явним керуванням пулом підключень PostgreSQL.

Реалізація:
- `PrismaService` наслідує `PrismaClient`
- створюється власний `pg.Pool` на основі `DATABASE_URL`
- пул передається в Prisma через `@prisma/adapter-pg`
- під час shutdown:
  - викликається `prisma.$disconnect()`
  - коректно закривається `pg.Pool`

Такий підхід забезпечує:
- передбачувану поведінку підключень
- контрольований lifecycle бази даних
- коректну роботу під час graceful shutdown

---

### Singleton PrismaService через глобальний DatabaseModule

Щоб уникнути створення декількох інстансів PrismaClient (і, відповідно, декількох пулів підключень), логіка доступу до БД винесена в окремий глобальний модуль.

Рішення:
- `DatabaseModule` позначений як `@Global()`
- `PrismaService` оголошений лише один раз у `providers` цього модуля
- feature-модулі не створюють власних інстансів PrismaService

Це дозволяє:
- гарантувати єдиний PrismaClient на весь застосунок
- уникнути надмірної кількості з’єднань з БД
- зменшити ризик прихованих runtime-помилок у модульній архітектурі NestJS

---

### Глобальна валідація та обробка помилок

Валідація вхідних запитів виконується глобально за допомогою ValidationPipe, що дозволяє тримати контролери чистими та забезпечує строгі API-контракти.

Налаштування:
- `whitelist: true`
- `forbidNonWhitelisted: true`
- `transform: true`
- `enableImplicitConversion: true`

Помилки бізнес-логіки та авторизації повертаються з коректними HTTP-статусами  
(наприклад: `401 Unauthorized`, `403 Forbidden`, `409 Conflict`).

Внутрішні помилки не розкривають деталей реалізації або структури бази даних.

---

### Correlation ID у відповідях API

У системі використовується correlation ID для ідентифікації та трасування кожного HTTP-запиту.

Один і той самий ідентифікатор присутній у двох формах:
- `X-Request-Id` — у HTTP response headers
- `requestId` — у тілі відповіді з помилкою

Обидва поля містять однакове значення та відображають один і той самий запит.

#### X-Request-Id (HTTP header)

X-Request-Id використовується для задач транспортного рівня:
- кореляція запитів у backend-логах
- інтеграція з проксі, API Gateway або load balancer
- трасування запитів незалежно від формату відповіді

Оскільки це HTTP-заголовок, він доступний для будь-якої відповіді (включно з помилками).

#### requestId (тіло відповіді)

requestId додається лише до відповідей з помилками як частина контракту API.

Він призначений для:
- frontend-клієнтів
- логування або відображення помилок на рівні застосунку
- подальшої діагностики та підтримки

Наявність requestId у тілі відповіді дозволяє працювати з ідентифікатором запиту без доступу до HTTP-headers.

#### Використання на різних рівнях

| Рівень | Поле |
|------|------|
| HTTP / інфраструктура | `X-Request-Id` |
| Backend-логування | `X-Request-Id` |
| Frontend / UI | `requestId` |
| Діагностика помилок | обидва |

#### Генерація та поширення

- Якщо клієнт передає `X-Request-Id`, система використовує його
- Якщо заголовок не передано — ідентифікатор генерується на backend
- Для помилкових відповідей correlation ID завжди присутній:
  - у headers (`X-Request-Id`)
  - у тілі відповіді (`requestId`)

Це забезпечує стабільну та передбачувану поведінку API.

---

### Чому requestId не додається до успішних відповідей

requestId навмисно не додається до body успішних відповідей.

Причини:
- контракти API залишаються чистими та сфокусованими на бізнес-даних
- frontend не зобов’язаний враховувати службові поля у кожному DTO
- спрощується подальша еволюція API (GraphQL, gRPC, events)

При цьому:
- `X-Request-Id` завжди доступний у headers
- для помилок додається додаткова діагностична інформація

---

### Підсумок по correlation ID

| Ситуація | Де присутній correlation ID |
|--------|------------------------------|
| Успішна відповідь | response headers |
| Помилка | headers + body |
| Swagger | задокументований як optional header |


---

## Ціноутворення та знижки

- Усі грошові розрахунки виконуються через `Prisma.Decimal`
- Округлення застосовується лише на фінальних грошових значеннях (ROUND_HALF_UP)
- Річна підписка має фіксовану знижку **17%**
- Промокоди застосовуються **лише для місячних підписок**
- Річна знижка та промокоди **не комбінуються**
- Якщо знижка не застосовується — клієнт отримує пояснювальну примітку в відповіді API

---

## Ідемпотентність

Створення підписки захищене заголовком `Idempotency-Key`.

### Детальніше про ідемпотентність (POST /subscriptions)

- Клієнт передає заголовок `Idempotency-Key` для безпечних повторних спроб (timeouts, retries, double-click).
- Ключ зберігається в таблиці `payment` та має унікальне обмеження.
- Якщо запит з таким ключем приходить повторно — API повертає вже створений результат без створення нових записів.
- Конкурентні запити з однаковим ключем обробляються через унікальний constraint у БД (race-safe): один запит виграє, інший виконує replay.
- У відповідь повертається ознака `idempotentReplay: true`, щоб клієнт міг відрізнити повторну відповідь.

---

## Автентифікація та доступ

- Мінімальна JWT-автентифікація (лише access token)
- Усі subscription-ендпоінти захищені JWT-guard
- Користувач має доступ тільки до власних ресурсів
- Refresh-токени не реалізовані свідомо (scope MVP)

---

## База даних та seed

- Усі primary key — **UUID**. Це свідоме рішення для публічного API, яке запобігає перебору ідентифікаторів та краще підходить для SaaS-систем і можливого горизонтального масштабування.
- Enum-типи зберігаються як PostgreSQL ENUM
- Seed-скрипт ініціалізує:
  - тарифні плани
  - промокоди
- Seed ідемпотентний та безпечний для повторного запуску

---

## API документація

Swagger UI доступний за адресою:

```

/api/docs

```

Swagger відкритий для перегляду,
але всі захищені ендпоінти все одно вимагають JWT-автентифікацію.

---

## Запуск проєкту

### Вимоги

- Node.js 18+
- Docker та Docker Compose

### Налаштування

```bash
npm install
npm run dev:setup
npm run start:dev
```

Команда `dev:setup`:

- запускає PostgreSQL
- застосовує міграції
- генерує Prisma-клієнт
- виконує seed-скрипт


### Змінні середовища

Створіть файл `.env` на основі наданого `.env.example`:

```bash
cp .env.example .env
```
### Docker

Docker використовується для запуску інфраструктурних залежностей (PostgreSQL) у локальному середовищі розробки.
Це забезпечує швидкий старт проєкту та однакове середовище без необхідності локальної установки БД.

Dockerfile для деплою API навмисно не додавався, оскільки це не вимагалося умовами тестового завдання
і залежить від конкретного production-оточення.

---

## Тестування

Unit-тести реалізовані лише для pricing-логіки,
оскільки це найбільш критична та детермінована частина системи.

```bash
npm run test
```

---

## Швидка перевірка через Swagger / Postman

1. `POST /api/auth/register` — зареєструвати користувача
2. `POST /api/auth/login` — отримати access token
3. У Swagger натиснути **Authorize** та вставити токен
4. `POST /api/pricing/calculate` — перевірити pricing (monthly / annual / promo)
5. `POST /api/subscriptions` з заголовком `Idempotency-Key`
6. Повторити той самий запит з тим самим `Idempotency-Key` — переконатися, що записи не дублюються та `idempotentReplay=true`
7. `GET /api/subscriptions` та `GET /api/subscriptions/:id`

---

## Додаткові примітки

- Платіжні провайдери реалізовані як mock-сервіси
- Активація підписки очікується асинхронно (як у production-системах)
- Проєкт сфокусований на коректності та зрозумілості реалізації

Архітектурні рішення свідомо спрямовані на баланс між простотою MVP та production-практиками,
щоб проєкт залишався зрозумілим, розширюваним і готовим до подальшого розвитку.
